// Web stub — PDF already downloaded via browser in pdf_builder
class WebUtils {
  static Future<void> openFile(String path) async {
    // No-op on web — browser handles download automatically
  }
}
