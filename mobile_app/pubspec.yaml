name: mobile_app
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # --- Humein in packages ki zaroorat hai ---
  google_fonts: ^6.2.1  # Premium fonts (Cinzel, Inter) ke liye
  animate_do: ^3.3.4     # Simple animations ke liye
  # ----------------------------------------

  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true